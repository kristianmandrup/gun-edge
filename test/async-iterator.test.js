import test from 'ava'

test('async iterator', async t => {

  // Unfortunately doesn't work :()

  // function* iterate(list) {
  //   list.map(n => {
  //     yield n
  //   })
  // }

  // for (let x of iterate(list)) {
  //   console.log(x)
  // }
})